const KB = 1024;
const MB = 1024 * KB;
const GB = 1024 * MB;
const TB = 1024 * GB;
const PB = 1024 * TB;

const WINSTON = 1;
const AR = 10 ** 12;

const SECOND = 1000;
const MINUTE = 60 * SECOND;
const HOUR = 60 * MINUTE;
const DAY = 24 * HOUR;
const WEEK = 7 * DAY;
const MONTH = 30 * DAY;
const YEAR = 365 * DAY;

module.exports = {
    KB, MB, GB, TB, PB,
    WINSTON, AR,
    SECOND, MINUTE, HOUR, DAY, WEEK, MONTH, YEAR
};